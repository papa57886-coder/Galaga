Pasos para jugar.
Desde la terminal de frontend colocar:
npm run dev

backend + Base de datos:

desde la terminal de la carpeta galaga:

1 crear entorno virtual

python -m venv venv

2.pip install -r requirements.txt

desde la terminal de backend: 
1 encender entorno

2 colocar:
python manage.py runserver


Nota: debe tener una base de datos postgre llamada galaba_db
y colocar en .env las credenciales del usuario con permisos